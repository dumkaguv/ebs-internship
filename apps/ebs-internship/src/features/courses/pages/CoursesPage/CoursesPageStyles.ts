import { createStyles } from "antd-style";

export const useCoursesPageStyles = createStyles(({ css }) => {
  return {
    leftSide: css`
      max-width: 305px;
      min-width: 305px;
      width: 100%;
    `,
  };
});
