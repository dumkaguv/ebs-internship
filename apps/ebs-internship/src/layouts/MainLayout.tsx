import { Header, Footer } from "@/components";
import { Layout } from "antd";

const MainLayout = () => {
  return (
    <Layout>
      <Header />
      <Footer />
    </Layout>
  );
};

export default MainLayout;
